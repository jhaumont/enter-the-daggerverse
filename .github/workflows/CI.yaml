name: CI hello
on:
  pull_request:
    branches:
      - 'Jules136'
  push:
    branches:
      - 'Jules136'

jobs:
  CI:
    name: Publish hello app
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
